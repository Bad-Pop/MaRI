package io.github.badpop.mari.domain.model.favorite.ads;

public record FavoriteAdSummary(String id,
                                String name,
                                String url,
                                FavoriteAdType type) {
}
